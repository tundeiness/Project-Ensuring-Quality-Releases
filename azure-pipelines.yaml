name: Azure Pipelines

trigger:
  branches:
    include:
      - main

pool:
  name: myAgentPool

variables:
  - group: azure-credentials       # ARM_CLIENT_ID, ARM_CLIENT_SECRET, etc.
  - group: azure-storage-key
  - group: vm-ssh-key              # ADMIN_SSH_PUBLIC_KEY
  - name: python_version
    value: '3.8.10'
  - name: azureServiceConnectionId
    value: 'ServiceConnex'
  - name: projectRoot
    value: '$(System.DefaultWorkingDirectory)'
  - name: environmentName
    value: 'test'
  - name: vmImageName
    value: 'devopsVM'

stages:
- stage: Build
  displayName: 'Provision Infrastructure'
  jobs:
  - job: BuildInfrastructure
    displayName: 'Infrastructure Provisioning'
    steps:
    # - task: Bash@3
    #   displayName: 'Install Postman & Newman'
    #   inputs:
    #     targetType: 'inline'
    #     script: |
    #       echo "Installing Postman dependencies..."

    # Uncomment below tasks if needed
    # - task: TerraformInstaller@0
    #   displayName: 'Install Terraform'
    #   inputs:
    #     terraformVersion: '1.5.7'

    # - task: Bash@3
    #   displayName: 'Write SSH Public Key'
    #   inputs:
    #     targetType: 'inline'
    #     script: |
    #       echo "$(ADMIN_SSH_PUBLIC_KEY)" > "$(projectRoot)/terraform/environments/test/id_rsa.pub"
    #       chmod 644 "$(projectRoot)/terraform/environments/test/id_rsa.pub"

    # - task: Bash@3
    #   displayName: 'Terraform Init'
    #   env:
    #     ARM_CLIENT_ID: $(ARM_CLIENT_ID)
    #     ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
    #     ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
    #     ARM_TENANT_ID: $(ARM_TENANT_ID)
    #   inputs:
    #     targetType: 'inline'
    #     script: |
    #       cd "$(projectRoot)/terraform/environments/test"
    #       terraform init \
    #         -backend-config="storage_account_name=tfstate5848c2ad9dc5" \
    #         -backend-config="container_name=tfstate" \
    #         -backend-config="key=test.terraform.tfstate" \
    #         -backend-config="resource_group_name=Azuredevops" \
    #         -backend-config="subscription_id=$(ARM_SUBSCRIPTION_ID)" \
    #         -backend-config="tenant_id=$(ARM_TENANT_ID)"

    # - task: Bash@3
    #   displayName: 'Create terraform.tfvars'
    #   env:
    #     ARM_CLIENT_ID: $(ARM_CLIENT_ID)
    #     ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
    #     ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
    #     ARM_TENANT_ID: $(ARM_TENANT_ID)
    #   inputs:
    #     targetType: 'inline'
    #     script: |
    #       cd "$(projectRoot)/terraform/environments/test"
    #       cat > terraform.tfvars << EOF
    #       subscription_id = "$(ARM_SUBSCRIPTION_ID)"
    #       client_id       = "$(ARM_CLIENT_ID)"
    #       client_secret   = "$(ARM_CLIENT_SECRET)"
    #       tenant_id       = "$(ARM_TENANT_ID)"
    #       location         = "West Europe"
    #       resource_group   = "Azuredevops"
    #       application_type = "myWebApp"
    #       virtual_network_name  = "devopsnet"
    #       address_space         = ["10.5.0.0/16"]
    #       address_prefix_test   = "10.5.1.0/24"
    #       vm_name     = "$(vmImageName)"
    #       admin_ssh_key = "$(ADMIN_SSH_PUBLIC_KEY)"
    #       EOF

    # - task: Bash@3
    #   displayName: 'Terraform Apply'
    #   env:
    #     ARM_CLIENT_ID: $(ARM_CLIENT_ID)
    #     ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
    #     ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)
    #     ARM_TENANT_ID: $(ARM_TENANT_ID)
    #   inputs:
    #     targetType: 'inline'
    #     script: |
    #       cd "$(projectRoot)/terraform/environments/test"
    #       terraform apply -var-file=terraform.tfvars -auto-approve

    - task: Bash@3
      displayName: 'Configure Selenium'
      inputs:
        targetType: 'inline'
        script: |
          sudo apt-get update
          sudo apt-get upgrade -y
          sudo apt-get install python3-pip unzip expect -y
          sudo apt-get install -y chromium-browser
          pip3 install selenium
          sudo rm -rf chromedriver*
          wget https://chromedriver.storage.googleapis.com/96.0.4664.18/chromedriver_linux64.zip
          unzip chromedriver_linux64.zip
          sudo mv chromedriver -f /usr/bin

- stage: Test
  displayName: 'API Testing Stage'
  jobs:
  - job: PostmanTests
    displayName: 'Postman Tests'
    pool:
      name: myAgentPool
    steps:
    - task: Bash@3
      displayName: 'Run Postman Tests and Export JUnit Results'
      inputs:
        targetType: 'inline'
        script: |
          /usr/bin/newman run automatedtesting/postman/StarterAPIs.json \
            --reporters cli,junit \
            --reporter-junit-export automatedtesting/postman/test-results.xml \
            --suppress-exit-code 1

    - task: PublishTestResults@2
      displayName: 'Publish Test Results'
      inputs:
        testResultsFormat: 'JUnit'
        testResultsFiles: 'automatedtesting/postman/test-results.xml'
        mergeTestResults: true



  - deployment: DeploySeleniumOnVM
    displayName: 'Deploy Selenium on VM'
    environment:
      name: $(vmImageName) 
      resourceType: VirtualMachine

    strategy:
      runOnce:
        deploy:  
          steps:  
          - task: Bash@3
            inputs: 
              targetType: 'inline'
              workingDirectory: '/home/adminuser'
              script: |
                #! /bin/bash
                sudo apt-get upgrade -y 
                sudo apt-get update -y
                sudo apt-get install python3-pip -y 
                sudo apt-get install unzip -y
                sudo wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
                sudo dpkg -i google-chrome-stable_current_amd64.deb
                sudo apt-get -f install -y
                sudo apt-get install -y unzip xvfb libxi6 libgconf-2-4
                sudo wget https://chromedriver.storage.googleapis.com/2.41/chromedriver_linux64.zip
                sudo unzip chromedriver_linux64.zip
                
                sudo apt install python3-pip
                sudo apt-get install -y chromium-browser 
                pip3 install selenium
                
                export PATH=$PATH:/home/adminuser:/bin
                sudo python3 '$(Pipeline.Workspace)/$(selenium)/ui_automation.py'

  - job: JMeterTests
    displayName: 'JMeter Tests'
    steps:
    - task: Bash@3
      displayName: 'Install JMeter'
      inputs:
        targetType: 'inline'
        script: |
          sudo apt-get install openjdk-11-jre-headless -y
          wget https://archive.apache.org/dist/jmeter/binaries/apache-jmeter-5.4.1.tgz -O jmeter.tgz
          tar -xzvf jmeter.tgz

    - task: Bash@3
      displayName: 'Run JMeter'
      inputs:
        targetType: 'inline'
        script: |
          apache-jmeter-5.4.1/bin/jmeter -n -t automatedtesting/jmeter/Starter.jmx \
            -l automatedtesting/jmeter/logs/test-output.csv \
            -e -f -o automatedtesting/jmeter/logs/test-log.html \
            -j automatedtesting/jmeter/logs/jmeter.log
